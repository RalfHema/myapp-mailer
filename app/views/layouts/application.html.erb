<!DOCTYPE html>
<html>
  <head>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta content="width=device-width,initial-scale=1.0" name="viewport" />
   <title>MyappMailer</title>
   <%= csrf_meta_tags %>
   <%= csp_meta_tag %>

   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
   <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body >

   <header>
      <%= render 'layouts/nav'%>
   </header>

      
   <div class="container">
      <%= bootstrap_flash %>
      <div>
         <button type="button" class="btn btn-primary"><%= link_to "Sign Up", new_user_registration_path %></button>
         <button type="button" class="btn btn-primary"><%= link_to "Sign In", new_user_session_path %></button>
         <button type="button" class="btn btn-primary"><%= link_to "Sign out", destroy_user_session_path, method: :delete %></button>
      </div>
      <br>

      <%= yield %>
   </div>
   <br>
   <br>
   <footer id="footer-bottom">
    <%= render 'layouts/footer'%>
   </footer>

</body>
</html>


